{
  "name": "ose-quench-ci",
  "version": "0.0.0",
  "private": true,
  "description": "Headless Playwright runner for the OSE Quench test suite in CI. Standalone so its Playwright dependency stays out of the system's build/runtime deps.",
  "type": "module",
  "scripts": {
    "quench": "node run-quench.mjs"
  },
  "dependencies": {
    "playwright": "1.60.0"
  }
}
